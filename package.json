{
  "name": "fake-data-generator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others 'deno task tsc' 'deno task vite'",
    "tsc": "tsc --watch",
    "vite": "vite build --watch",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "sass": "^1.79.5",
    "typescript": "^5.5.3",
    "vite": "^5.4.8"
  }
}
